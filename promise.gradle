def version_suffixed = { String version ->  project.hasProperty("SUFFIX") && project.SUFFIX.length() > 0
        ? "${version}-${project.SUFFIX}"
        : "${version}" }

def version="2.7.4"
rootProject.ext.version = version_suffixed(version)
